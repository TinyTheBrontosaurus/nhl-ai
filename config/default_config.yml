mode: train
input:
  scenarios:
    - scenario: BufAtChi-Faceoff.state
      scorekeeper: point-per-frame
  metascorekeeper: summer
movie:
  enabled: generations
  stoppage-time-s: 5.0
render-live: False
nproc: null