mode: train
input:
  scenarios:
    - name: basic
      state: BufAtChi-Faceoff.state
      scorekeeper: point-per-frame
  metascorekeeper: summer
movie:
  enabled: generations
  stoppage-time-s: 5.0
render-live: False
nproc: null